# Server
PORT=4000

# Database (matches docker-compose.yml defaults)
DATABASE_URL="postgresql://panel_user:panel_pass@localhost:5432/panel_db?schema=public"

# Auth
JWT_SECRET="change-me"
JWT_EXPIRES_IN="7d"

# Cache / queue (Redis)
REDIS_HOST="localhost"
REDIS_PORT=6379
