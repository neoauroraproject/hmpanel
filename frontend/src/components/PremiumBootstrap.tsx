"use client";

/**
 * No-op in Community — premium bundle registers menus/routes after install.
 * Dev: use dev-link-community.js + premium overlays for full bootstrap.
 */
export function PremiumBootstrap() {
  return null;
}
